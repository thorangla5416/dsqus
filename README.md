# dsqus

A minimal Python package that helps in scoring your dataset quality by evaluating differeny parameters. Before you run the actual model training, this dataset quality scoring approach will label your data how good it is in performing the model training. Let's save your model training cost by analyzing the dataset itself, rather than training bulky data which is not novice and lacks uniqueness, novelty`.
